# Artifacts

Binary build artifacts are not committed to this repository.

The original local archive `stickytags-aarch64-artifacts.tar.gz` was omitted intentionally. Rebuild artifacts by following `docs/build-and-run.md`, then verify checksums and binary evidence with the files under `Experiment/manifests/` and `Experiment/logs/`.
